# Minimal requirements for Cloud Shell deployment
# This file contains only essential packages to avoid storage issues

# Core ML and Data Science (essential only)
scikit-learn>=1.0.0
joblib>=1.0.0
numpy>=1.20.0
pandas>=1.3.0

# Basic visualization (lightweight)
matplotlib>=3.5.0

# Testing (essential)
pytest>=6.0.0

# Code quality (lightweight)
black>=22.0.0

# Additional ML utilities (essential)
scipy>=1.7.0

# DVC (lightweight - no cloud extras initially)
dvc>=3.0.0